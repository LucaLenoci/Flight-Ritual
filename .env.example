# SQLite file used by Prisma. Safe to commit the example; real .env is gitignored.
DATABASE_URL="file:./dev.db"

# Dev-mode single-user auth. Replace with a real identity provider before
# enabling multi-user access. Never commit real secrets here.
DEV_SESSION_SECRET="replace-with-a-random-32-byte-value"
